# Инструкция по настройке улучшенного выбора местоположения

## Что было реализовано

Добавлены три способа указания местоположения:
1. **Отправка текущего местоположения** (как было)
2. **Выбор точки на интерактивной карте** через Telegram Web App
3. **Ввод адреса текстом** с автоматическим геокодингом через Яндекс.Геокодер API

## Настройка

### 1. Получение API ключа Яндекс.Геокодера

1. Зайдите на https://developer.tech.yandex.ru/
2. Создайте новый ключ для API Яндекс.Геокодера
3. Скопируйте ключ

### 2. Настройка переменных окружения

Добавьте в ваш `.env` файл:

```env
YANDEX_GEOCODER_API_KEY=ваш_ключ_здесь
WEB_APP_URL=https://naumrabota.ru/web_apps/location_picker.html
```

**Примечание:** Замените `https://naumrabota.ru` на ваш домен, если он отличается.

### 3. Размещение Web App на сервере

1. Скопируйте файл `web_apps/location_picker.html` на ваш веб-сервер
2. Убедитесь, что файл доступен по HTTPS по адресу, указанному в `WEB_APP_URL`
3. Проверьте, что nginx настроен для обслуживания статических файлов из директории `web_apps/`

Пример настройки nginx:

```nginx
location /web_apps/ {
    alias /opt/naumrabota/web_apps/;
    try_files $uri $uri/ =404;
}
```

### 4. Перезапуск бота

После настройки перезапустите бота:

```bash
sudo supervisorctl restart naumrabota-bot
```

## Проверка работы

1. Создайте новое резюме или вакансию
2. После ввода города вы должны увидеть три кнопки:
   - 📍 Отправить геопозицию
   - 🗺 Выбрать на карте
   - ✏️ Ввести адрес
3. Протестируйте все три способа

## Важные замечания

- **Без API ключа** функция ввода адреса не будет работать, но остальные два способа будут доступны
- **Web App требует HTTPS** - убедитесь, что ваш сервер имеет валидный SSL сертификат
- Если Web App не открывается, проверьте URL в конфигурации и доступность файла на сервере

## Устранение неполадок

### Web App не открывается
- Проверьте, что URL правильный и файл доступен
- Убедитесь, что используется HTTPS
- Проверьте логи nginx: `sudo tail -f /var/log/nginx/error.log`

### Геокодинг не работает
- Проверьте правильность API ключа в `.env`
- Убедитесь, что ключ активен в личном кабинете Яндекс
- Проверьте лимиты API (бесплатный тариф имеет ограничения)

### Ошибки в боте
- Проверьте логи: `sudo supervisorctl tail -f naumrabota-bot`
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
